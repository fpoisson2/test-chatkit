services:
  backend:
    image: chatkit-backend:latest
    pull_policy: build
    build:
      context: .
      dockerfile: backend/Dockerfile
      target: final
      # Cache inline pour BuildKit (survit mieux que le cache par défaut)
      args:
        BUILDKIT_INLINE_CACHE: 1
    working_dir: /workspace/backend
    command: >-
      uvicorn server:app --host 0.0.0.0 --port 8000 --reload --log-level warning --no-access-log
    volumes:
      - .:/workspace
    environment:
      MODEL_PROVIDER: ${MODEL_PROVIDER:-openai}
      MODEL_API_BASE: ${MODEL_API_BASE:-}
      MODEL_API_KEY_ENV: ${MODEL_API_KEY_ENV:-}
      LITELLM_API_BASE: ${LITELLM_API_BASE:-}
      LITELLM_API_KEY: ${LITELLM_API_KEY:-}
      CHATKIT_API_BASE: ${CHATKIT_API_BASE:-https://api.openai.com}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      CHATKIT_WORKFLOW_ID: ${CHATKIT_WORKFLOW_ID:-}
      DATABASE_URL: "${DATABASE_URL:?DATABASE_URL is required}"
      CELERY_BROKER_URL: ${CELERY_BROKER_URL:-redis://redis:6380/0}
      CELERY_RESULT_BACKEND: ${CELERY_BROKER_URL:-redis://redis:6380/0}
      AUTH_SECRET_KEY: "${AUTH_SECRET_KEY:?AUTH_SECRET_KEY is required}"
      ACCESS_TOKEN_EXPIRE_MINUTES: ${ACCESS_TOKEN_EXPIRE_MINUTES:-120}
      ADMIN_EMAIL: ${ADMIN_EMAIL:-}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-http://localhost:5183}
      ENVIRONMENT: ${ENVIRONMENT:-development}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      LOG_FORMAT: ${LOG_FORMAT:-console}
      LITELLM_LOG_LEVEL: ${LITELLM_LOG_LEVEL:-INFO}
      CHATKIT_HOSTED_BROWSER_HEADLESS: ${CHATKIT_HOSTED_BROWSER_HEADLESS:-}
      CHATKIT_HOSTED_BROWSER_DEBUG_HOST: ${CHATKIT_HOSTED_BROWSER_DEBUG_HOST:-127.0.0.1}
      CHATKIT_HOSTED_BROWSER_DEBUG_PORT: ${CHATKIT_HOSTED_BROWSER_DEBUG_PORT:-}
      CHATKIT_HOSTED_BROWSER_PRELAUNCH: ${CHATKIT_HOSTED_BROWSER_PRELAUNCH:-true}
      SIP_BIND_HOST: ${SIP_BIND_HOST:-0.0.0.0}
      SIP_BIND_PORT: ${SIP_BIND_PORT:-40118}
      SIP_CONTACT_HOST: ${SIP_CONTACT_HOST:-192.168.1.116}
      SIP_TRANSPORT: ${SIP_TRANSPORT:-udp}
      LTI_TOOL_PRIVATE_KEY: ${LTI_TOOL_PRIVATE_KEY:-}
    networks:
      - app-network
    depends_on:
      - db
      - redis
  frontend:
    image: node:20
    working_dir: /workspace/frontend
    command: >-
      sh -c "npm install && npm run dev -- --host 0.0.0.0 --port 5183"
    volumes:
      - .:/workspace
      - frontend-node_modules:/workspace/frontend/node_modules
    environment:
      VITE_BACKEND_URL: ${VITE_BACKEND_URL:-http://localhost:8000}
      VITE_PORT: 5183
      VITE_HMR_HOST: ${VITE_HMR_HOST:-localhost}
      VITE_ALLOWED_HOSTS: ${VITE_ALLOWED_HOSTS:-}
      VITE_HMR_CLIENT_PORT: ${VITE_HMR_CLIENT_PORT:-80}
      VITE_HMR_PROTOCOL: ${VITE_HMR_PROTOCOL:-ws}
      VITE_CHATKIT_API_URL: ${VITE_CHATKIT_API_URL:-/api/chatkit}
      VITE_CHATKIT_DOMAIN_KEY: ${VITE_CHATKIT_DOMAIN_KEY:-}
      VITE_CHATKIT_FORCE_HOSTED: ${VITE_CHATKIT_FORCE_HOSTED:-}
      VITE_CHATKIT_SKIP_DOMAIN_VERIFICATION: ${VITE_CHATKIT_SKIP_DOMAIN_VERIFICATION:-}
      VITE_CHATKIT_UPLOAD_STRATEGY: ${VITE_CHATKIT_UPLOAD_STRATEGY:-}
      VITE_CHATKIT_DIRECT_UPLOAD_URL: ${VITE_CHATKIT_DIRECT_UPLOAD_URL:-}
      VITE_USE_MOCK_API: ${VITE_USE_MOCK_API:-false}
    networks:
      - app-network
    depends_on:
      - backend
  nginx:
    image: nginx:alpine
    container_name: nginx
    restart: unless-stopped
    environment:
      APP_DOMAIN: ${APP_DOMAIN:-localhost}
    command: >-
      /bin/sh -c "APP_DOMAIN=${APP_DOMAIN:-localhost}; envsubst '$$APP_DOMAIN' < /etc/nginx/conf.d/site.conf.template > /etc/nginx/conf.d/default.conf && exec nginx -g 'daemon off;'"
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d/site.conf.template:/etc/nginx/conf.d/site.conf.template:ro
    networks:
      - app-network
    depends_on:
      - backend
      - frontend
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-chatkit}
      POSTGRES_USER: ${POSTGRES_USER:-chatkit}
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - app-network
  redis:
    image: redis:7-alpine
    command: redis-server --port 6380
    volumes:
      - redis-data:/data
    networks:
      - app-network
  cloudflared:
    image: cloudflare/cloudflared:latest
    command: >
      sh -c "
      if [ -z \"$$CLOUDFLARED_TOKEN\" ]; then
        echo 'CLOUDFLARED_TOKEN is not set. Cloudflare Tunnel is disabled. Entering idle mode...';
        while true; do sleep 3600; done;
      else
        echo 'CLOUDFLARED_TOKEN found. Starting Cloudflare Tunnel...';
        exec cloudflared tunnel --no-autoupdate run --token $$CLOUDFLARED_TOKEN;
      fi
      "
    restart: unless-stopped
    environment:
      CLOUDFLARED_TOKEN: ${CLOUDFLARED_TOKEN:-}
    networks:
      - app-network
  celery-worker:
    image: chatkit-backend:latest
    pull_policy: never
    working_dir: /workspace/backend
    command: >-
      celery -A app.celery_app worker --loglevel=info --concurrency=2
    volumes:
      - .:/workspace
    environment:
      # Model & AI Configuration
      MODEL_PROVIDER: ${MODEL_PROVIDER:-openai}
      MODEL_API_BASE: ${MODEL_API_BASE:-}
      MODEL_API_KEY_ENV: ${MODEL_API_KEY_ENV:-}
      LITELLM_API_BASE: ${LITELLM_API_BASE:-}
      LITELLM_API_KEY: ${LITELLM_API_KEY:-}
      CHATKIT_API_BASE: ${CHATKIT_API_BASE:-https://api.openai.com}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      CHATKIT_WORKFLOW_ID: ${CHATKIT_WORKFLOW_ID:-}
      # Database
      DATABASE_URL: "${DATABASE_URL:?DATABASE_URL is required}"
      # Celery
      CELERY_BROKER_URL: ${CELERY_BROKER_URL:-redis://redis:6380/0}
      CELERY_RESULT_BACKEND: ${CELERY_BROKER_URL:-redis://redis:6380/0}
      # Auth (required by app initialization)
      AUTH_SECRET_KEY: "${AUTH_SECRET_KEY:?AUTH_SECRET_KEY is required}"
      ACCESS_TOKEN_EXPIRE_MINUTES: ${ACCESS_TOKEN_EXPIRE_MINUTES:-120}
      ADMIN_EMAIL: ${ADMIN_EMAIL:-}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-}
      # CORS & API
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-http://localhost:5183}
      # Logging
      ENVIRONMENT: ${ENVIRONMENT:-development}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      LOG_FORMAT: ${LOG_FORMAT:-console}
      LITELLM_LOG_LEVEL: ${LITELLM_LOG_LEVEL:-INFO}
      # SIP (if needed by app initialization)
      CHATKIT_HOSTED_BROWSER_HEADLESS: ${CHATKIT_HOSTED_BROWSER_HEADLESS:-}
      CHATKIT_HOSTED_BROWSER_DEBUG_HOST: ${CHATKIT_HOSTED_BROWSER_DEBUG_HOST:-127.0.0.1}
      CHATKIT_HOSTED_BROWSER_DEBUG_PORT: ${CHATKIT_HOSTED_BROWSER_DEBUG_PORT:-}
      CHATKIT_HOSTED_BROWSER_PRELAUNCH: ${CHATKIT_HOSTED_BROWSER_PRELAUNCH:-true}
      SIP_BIND_HOST: ${SIP_BIND_HOST:-0.0.0.0}
      SIP_BIND_PORT: ${SIP_BIND_PORT:-40118}
      SIP_CONTACT_HOST: ${SIP_CONTACT_HOST:-192.168.1.116}
      SIP_TRANSPORT: ${SIP_TRANSPORT:-udp}
      # LTI Configuration
      LTI_TOOL_PRIVATE_KEY: ${LTI_TOOL_PRIVATE_KEY:-}
    networks:
      - app-network
    depends_on:
      - db
      - redis

networks:
  app-network:
    driver: bridge

volumes:
  frontend-node_modules:
  postgres-data:
  redis-data:
