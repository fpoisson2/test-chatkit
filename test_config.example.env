# Configuration pour les tests d'appels entrants
# Copiez ce fichier en test_config.env et remplissez vos valeurs

# === Configuration SIP ===
SIP_URI=sip:utilisateur@domaine.com
REGISTRAR_URI=sip:domaine.com
SIP_USERNAME=utilisateur
SIP_PASSWORD=motdepasse

# Transport SIP (UDP ou TCP)
SIP_TRANSPORT=UDP

# Port SIP (5060 par défaut)
SIP_PORT=5060

# === Configuration OpenAI (pour test avec bridge) ===
OPENAI_API_KEY=sk-...

# Modèle à utiliser
OPENAI_MODEL=gpt-4o-realtime-preview

# Voix à utiliser (alloy, echo, shimmer, ash, ballad, coral, sage, verse)
OPENAI_VOICE=alloy

# Instructions pour l'assistant (IMPORTANT: pas d'espace autour du =)
OPENAI_INSTRUCTIONS="Vous êtes un assistant vocal de test. Répondez brièvement aux questions en français."
