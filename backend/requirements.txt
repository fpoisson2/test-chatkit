# Core dependencies
asyncssh
websockify>=0.11.0  # WebSocket-to-TCP proxy for VNC (official noVNC project)
openai-agents[litellm]
# aiosip - Removed due to Python 3.11+ incompatibility (@asyncio.coroutine deprecated)
# pjsua2  # PJSIP stack - Compiled from source in Dockerfile (avoid pip sdist issues)
soxr>=0.3.0  # High-quality audio resampling (VHQ mode for telephony)
numpy>=1.24.0  # Required by soxr for audio processing
psutil>=5.9.0  # System and process monitoring (used for RTP port diagnostics)
fastapi
slowapi>=0.1.9  # Rate limiting for FastAPI (uses existing Redis for storage)
openai
openai-chatkit @ file:../chatkit-python
cryptography
uvicorn[standard]
python-dotenv
httpx
playwright
sqlalchemy
psycopg[binary]
PyJWT
pylti1p3
email-validator

# Vector store - uses OpenAI Embeddings API (no heavy ML dependencies needed)
pgvector

# Background tasks
celery[redis]>=5.3.0
redis>=5.0.0

# Observabilité
structlog>=24.1.0

# Outils de qualité de code
black>=24.4.0
isort>=5.13.0
mypy>=1.10.0
ruff>=0.4.0

# Tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# Document conversion
python-docx>=1.1.0  # For DOCX file handling and validation
