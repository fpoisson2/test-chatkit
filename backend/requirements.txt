# Core dependencies
openai-agents
fastapi
openai
openai-chatkit @ file:../chatkit-python
uvicorn[standard]
python-dotenv
httpx
sqlalchemy
psycopg[binary]
PyJWT
email-validator

# Vector store - uses OpenAI Embeddings API (no heavy ML dependencies needed)
pgvector

# Alternative: Local embeddings (heavy - ~500MB with PyTorch)
# Uncomment if you prefer local embeddings instead of OpenAI API
# sentence-transformers

# Outils de qualité de code
black>=24.4.0
isort>=5.13.0
mypy>=1.10.0
ruff>=0.4.0
