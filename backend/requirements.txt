# Core dependencies
openai-agents
fastapi
openai
openai-chatkit @ file:../chatkit-python
uvicorn[standard]
python-dotenv
httpx
sqlalchemy
psycopg
PyJWT
email-validator

# Vector store (optional - only needed for semantic search features)
# Uncomment the lines below if you need vector store functionality
# pgvector
# sentence-transformers
